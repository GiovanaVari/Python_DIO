cores = ["vermelho", "azul", "verde", "azul"]

print(cores.count("vermelho"))                  # Mostra quantas * aparece na lista a cor correspondente (1)
print(cores.count("azul"))                      # Mostra quantas * aparece na lista a cor correspondente (2)
print(cores.count("verde"))                     # Mostra quantas * aparece na lista a cor correspondente (1)